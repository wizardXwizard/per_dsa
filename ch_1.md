Here it is:

**Level 1 — Python fluency**

1. Count words in a sentence, return the most frequent word
2. Find all pairs in a list that sum to a target — no built-in combinations
3. Reverse words in a sentence without reversing letters in each word
4. Return the first non-repeating character in a string
5. Check if two strings are anagrams
6. Move all zeros to the end of a list while preserving order of non-zero elements
7. Find the longest common prefix among a list of strings
8. Check if a number is a palindrome without converting to string
9. Flatten a nested list of arbitrary depth
10. Group a list of strings by their length

**Level 2 — Logic and data structures**

11. Implement a stack with push, pop, peek, is_empty
12. Implement a queue using two stacks
13. Check if a string with brackets is balanced
14. Return the Nth Fibonacci number using iteration only
15. Find the duplicate in a sorted list with one duplicate — O(n) time, O(1) space
16. Implement binary search from scratch
17. Find days until warmer temperature for each day in a temperature list
18. Write a Caesar cipher
19. Count islands in a 2D grid of 0s and 1s
20. Implement a basic LRU cache

**Level 3 — Python for DevOps**

21. Read a log file line by line, count ERROR occurrences per hour
22. Parse a JSON config and validate required keys exist
23. Retry a failing operation N times with exponential backoff
24. Filter Kubernetes pod dicts not in Running state, sort by restart count
25. Decorator that times how long a function takes
26. Simple rate limiter — function callable only N times per second
27. Check if a list of URLs are reachable, return status codes
28. Get a value from a nested dict using dot notation like "spec.containers.0.name"
29. Context manager that redirects stdout to a file
30. Simple pub/sub system — publishers emit events, subscribers receive them

No AI while solving. Google syntax if needed. The struggle is the point.
